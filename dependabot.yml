version: 2
updates:
  # Enable version updates for yarn
  - package-ecosystem: 'yarn'
    # Look for `package.json` and `lock` files in the `root` directory
    directory: '/'
    # Check the yarn registry for updates daily
    schedule:
      interval: 'daily'
